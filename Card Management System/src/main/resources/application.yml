spring:
  application:
    name: Card Management System

  datasource:
    url: jdbc:mysql://localhost:3306/card_management_system?serverTimezone=UTC
    username: root
    password: admin

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    open-in-view: false

server:
  port: 8080